services:
  app:
    build: .
    ports:
      - "3100:3100"
    restart: unless-stopped
    environment:
      # Change this to any strong token you want — then share the URL:
      # http://your-server:3100/?token=YOUR_TOKEN_HERE
      - ACCESS_TOKEN=anon-xK9mP2vL7nQ4rZ8wB3
